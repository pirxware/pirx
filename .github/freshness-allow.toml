# Packages exempt from the 7-day freshness gate.
#
# Use ONLY for security-motivated upgrades where the old version has a
# known advisory and the fix version was just published.  Remove the
# entry once the version is older than 7 days.
#
# Format:  name = "version"    (one entry per line)

# RUSTSEC-2026-0176: OOB read in nth/nth_back for PyList/PyTuple iterators
# RUSTSEC-2026-0177: missing Sync bound on PyCFunction::new_closure
pyo3 = "0.29.0"
pyo3-build-config = "0.29.0"
pyo3-ffi = "0.29.0"
pyo3-macros = "0.29.0"
pyo3-macros-backend = "0.29.0"
